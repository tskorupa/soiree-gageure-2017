<% content_for(:content_adjacent_to_sidebar) do %>
  <%= render(partial: 'results/index_header') %>
  <%= render(
    'results/drawn_ticket',
    lottery_id: @lottery.id,
    ticket: @ticket,
  ) %>
  <%= render(partial: 'results/fullscreen' ) %>
<% end %>
