<div class='page-header'>
  <div class='row-fluid'>
    <h1>
      <%= @guests_listing.title %>
      <div class='pull-right'>
        <%= link_to(
          @guests_listing.new_guest_button_name,
          new_guest_path,
          class: 'btn btn-primary',
        ) %>
      </div>
    </h1>
  </div>
</div>
