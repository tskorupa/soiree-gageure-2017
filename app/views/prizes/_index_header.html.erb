<div class='page-header'>
  <h1><%= t(
    '.title',
    default: Prize.model_name.human.pluralize(I18n.locale).titleize,
  ) %></h1>
  <div class='row'>
    <div class='pull-right'>
      <%= link_to(
        t(
          'prizes.new.title',
          default: [:'helpers.titles.new', 'New %{model}'],
          model: Prize.model_name.human.downcase,
        ),
        new_lottery_prize_path(@lottery),
        class: 'btn btn-primary',
      ) %>
    </div>
  </div>
</div>
